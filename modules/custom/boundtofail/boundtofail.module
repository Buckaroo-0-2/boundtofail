<?php

// Defines twig file for mobile_menu.
function mobile_menu_twig_theme($existing, $type, $theme, $path) {
  return [
    'mobile_menu_template' => [
      #'variables' => ['test_var' => NULL],
    ],
  ];
}
